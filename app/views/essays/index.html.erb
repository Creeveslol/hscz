<h1>Listing essays</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th>Topic</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @essays.each do |essay| %>
  <tr>
    <td><%= essay.title %></td>
    <td><%= essay.content %></td>
    <td><%= essay.topic %></td>
    <td><%= link_to 'Show', essay %></td>
    <td><%= link_to 'Edit', edit_essay_path(essay) %></td>
    <td><%= link_to 'Destroy', essay, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Essay', new_essay_path %>
